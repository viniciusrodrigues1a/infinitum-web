enum RoutesEnum {
  HOME = "/",
  DASHBOARD = "/dashboard",
  PROJECTS = "/projects",
  PROJECT = "/project/:projectId",
  ACCEPT_INVITATION = "/invitation/:invitationToken",
}

export default RoutesEnum;
